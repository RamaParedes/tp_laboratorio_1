#include <stdio.h>
    // Biblioteca con las funciones matematicas usadas y pedidas

    float suma(float, float);
    float resta(float, float);
    float division(float, float);
    float multiplicacion(float, float);
    int factorial(int);
